interface:
  display_name: "T3 Code Stack Rebuild"
  short_description: "Assemble, publish, and install the T3 Code stack"
  default_prompt: "Use $rebuild-t3code-stack to incorporate inventoried topics into the integration manifest, verify the assembled branch, repin Nix, and activate it."
