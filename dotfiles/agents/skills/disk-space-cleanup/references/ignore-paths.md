# Disk scan exclusions

Keep these machine-specific mountpoints out of root and home scans. Prefer
one-filesystem scans as the primary guardrail.

- `/Volumes/Extreme SSD`
- `/private/var/lib/gitea-runner-external`
